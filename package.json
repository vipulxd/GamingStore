{
  "name": "gasto",
  "version": "0.1.1",
  "description": "Gamer's Stop",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
   
    "build":"cd client && npm run build",
    "install-client":"cd client && npm install install",
    "heroku-postbuild":"npm run install-client && npm run build",
    "start": "node server.js"
  },
  "author": "marcrove",
  "license": "MIT",
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-validator": "^6.11.1",
    "http": "0.0.1-security",
    "mongoose": "^5.12.10",
    "nodemon": "^2.0.7",
    "react-redux": "^7.2.4",
    "redux": "^4.1.0"
  }
}
